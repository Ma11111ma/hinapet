ENV=development
APP_ENV=local
# Supabase/PostgreSQL（各自のローカルまたはDockerに合わせて設定）
DATABASE_URL=postgresql+psycopg2://postgres:maiwakeharumiki@db:5432/pet_db

# Stripe（テスト用キーを使用）
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID_MONTHLY=
FRONTEND_URL=http://localhost:3000
STRIPE_WEBHOOK_SECRET=


# 認証バイパス（暫定）：1=ON / 0=OFF
# 開発中は必要に応じて 1 にしてください。既定は安全側で 0。
AUTH_BYPASS=0

# Firebase Admin（PRIVATE_KEY は \n に置換してダブルクォートで囲む）
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=""

QR_BASE_URL=
NODE_ENV=development
